# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubWorkflow (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_build_wpf --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: build_wpf

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/build_forms.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Packages.props
      - xunit.runner.json
      - 'src/Prism.Core/**'
      - 'tests/Prism.Core.Tests/**'
      - 'src/Wpf/**'
      - 'tests/Wpf/**'

jobs:
  build-prism-wpf:
    name: Build Prism.Wpf
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: false
          fetch-depth: 0
      - name: 'Setup .NET 6.0.x'
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 6.0.x
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: build-prism.wpf-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd Compile --solution PrismLibrary_Wpf.slnf'
        run: ./build.cmd Compile --solution PrismLibrary_Wpf.slnf
