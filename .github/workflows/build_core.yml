# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubWorkflow (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_build_core --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: build_core

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/build_core.yml
      - Directory.Build.props
      - Directory.Build.targets
      - xunit.runner.json
      - 'src/Prism.Core/**'
      - 'tests/Prism.Core.Tests/**'

jobs:
  build-prism-core:
    name: Build Prism.Core
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: false
          fetch-depth: 0
      - name: 'Setup .NET 6.0.x'
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 6.0.x
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: build-prism.core-${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd Compile --solution PrismLibrary_Core.slnf'
        run: ./build.cmd Compile --solution PrismLibrary_Core.slnf
